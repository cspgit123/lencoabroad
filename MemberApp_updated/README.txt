MemberApp - Deploy instructions (with password login)
----------------------------------------------------
1) Install dependencies: npm install
2) Run locally: node server.js (visit http://localhost:3000)
3) Deploy to Render or Railway:
   - Build: npm install
   - Start: npm start
Notes:
   - Login requires username + password stored in XLSX.
   - Replace placeholder images in /public/images/ with your own.
